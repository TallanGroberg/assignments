import React from 'react'

const Controls = (props) => {

  const {addJerry } = props

  return (
    <>
      {/* { !addJerry.includes("jerry") && <button onClick={addJerry}>add Jerry</button>} */}

    </>
    )
}

export default Controls